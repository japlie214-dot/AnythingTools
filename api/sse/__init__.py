# api/sse/__init__.py
"""SSE streaming subsystem for AnythingTools.

Exports the projector (async generator), envelope (wire-format serializer),
phase deriver, decoder (structured-payload extractor), shutdown registry,
and log-notify bus.
"""
